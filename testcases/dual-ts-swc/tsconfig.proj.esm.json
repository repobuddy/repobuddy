// This is used for files in the project that are used by tools that are run as ESM.
{
	"compilerOptions": {
		"composite": true,
		"module": "Node16",
		// since it is used by tools only,
		// it is fine to enable json support for simplicity.
		"resolveJsonModule": true,
		"strict": true
	}
}
