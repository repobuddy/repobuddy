# tooling

Build, check, and release the monorepo: Turborepo task graph, Biome and ESLint checks, Changesets
versioning and release, Renovate dependency policy, and the CI workflows that run them.

No units are backfilled yet. This folder exists so a tooling concept has a declared home rather than
landing in `design/` by default.
