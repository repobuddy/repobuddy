{"kind":"leash","cr-ref":"github-594","leash":"auto-spec","by":"derived","approach":["revise the shipped merge-dep-prs skill so its merge oracle is reach-covers-radius","specify only the merge gate; the rest of the skill stays on the backfill worklist","add the README the skill was shipping without"]}
{"kind":"gate","cr-ref":"github-594","gate":"spec","verdict":"approve","by":"agent","why":"aced spec-validator round 3 returned ALIGNED with oracle-spec, builder-spec union aced-builder-spec, and architect-spec all PASS. Rounds 1 and 2 blocked; round 1 on an empty governance declaration, round 2 on an uncovered CFG edge, an extension with no path, and two scenarios a memorizer passed on restatement. All were fixed at the graph and re-derived. The two non-blocking round-3 findings were applied before the freeze."}
{"kind":"correction","cr-ref":"github-594","correction-kind":"judge-iteration","cause":"coverage-gap","why":"the drawn control-flow graph reconverged two enumeration routes on the resolution check but the scenario map bound only one of them, leaving the in-repo consumer check unspecified; a stated extension also had no path in the graph"}
{"kind":"correction","cr-ref":"github-594","correction-kind":"judge-iteration","cause":"governance-preflight-missing","cause-candidate":true,"why":"the inline spec-producer dispatched the first cold judge without declaring the governances it had loaded, which is a stop condition before any lens runs"}
{"kind":"gate","cr-ref":"github-594","gate":"impl","verdict":"approve","by":"agent","why":"aced impl-judge passed all 16 frozen scenarios by static inspection of the shipped skill against each scenario's Given/When/Then; both Scenario Outlines passed every row. Cross-references across the renumbered steps all resolve. No test suite ran — there is no harness for this skill and no ACED result on disk."}
{"kind":"followup","cr-ref":"github-594","class":"backlog","note":"Stand up an ACED run policy for merge-dep-prs. The skill has no eval.md and no result under .agents/aced/results/, so validate-skill warns and every impl-gate judgment is static reading rather than measurement over runs."}
{"kind":"followup","cr-ref":"github-594","class":"backlog","note":"Thin the recall surface in the merge-dep-prs SKILL.md worked example. Its incident apparatus overlaps three frozen scenarios' Givens closely enough that a config-quoting agent could pattern-match them out of its own instructions; the human-facing README already carries the incident in full, so the skill could keep the mechanism and drop the distinguishing counts."}
{"kind":"followup","cr-ref":"github-594","class":"backlog","note":"The merge-dep-prs description frontmatter still reads 'fixes CI, handles changesets, never merges release PRs' and never names the gate, now the skill's headline behavior. Out of scope here (Fit: partial, no trigger scenarios), but the description is what a harness matches on."}
