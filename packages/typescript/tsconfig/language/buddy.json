{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    // NodeJS +16.x support all (but one, which no NodeJS support) features in ES2022
    "target": "ES2022",
    // Emit ECMAScript-standard-compliant class fields.
    "useDefineForClassFields": true
  }
}