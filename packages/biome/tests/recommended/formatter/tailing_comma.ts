export type Foo = {
	a: string
	b: string
}

export function foo(
	apple: number,
	banana: number,
	cantalope: number,
	dingle: number,
	espresso: number,
	funny_joke: number,
) {
	return apple + banana + cantalope + dingle + espresso + funny_joke
}
