import { defineTransform } from './transform.js'

describe(`${defineTransform.name}()`, () => {
  it.todo('use ts-jest cjs there are ts|tsx|cts|mts files in source folder and type is not module')
  it.todo('use ts-jest esm there are ts|tsx|cts|mts files in source folder and type is module')
})
