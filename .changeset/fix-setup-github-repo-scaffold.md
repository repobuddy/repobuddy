---
---

Fix `setup-github-repo` skill: correct `scaffold-workflows.mts` argument parsing when `--workflows` is omitted, and use `--enable-merge-commit=false` instead of invalid `--no-allow-merge-commit` in `gh repo edit`.
