---
'repobuddy': patch
---

Publish the `templates` directory. The `files` allowlist named `template` (singular), which matched nothing on disk, so no template file was included in the published package.
