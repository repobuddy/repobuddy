---
'repobuddy': minor
---

Add `buddy release-age <status|lift|restore|open-pr>` and `buddy env`. They run the same code, with the same flags and output, as the `min-release-age` and `init-buddy` skill scripts.
