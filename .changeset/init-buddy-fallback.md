---
"repobuddy": patch
---

Point `add-badges`, `create-issue`, `merge-dep-prs`, `setup-github-pages`, `setup-github-repo`, and `setup-npm-trusted-publishing` at the `init-buddy` skill when `gh`/`glab` is missing or not logged in.
