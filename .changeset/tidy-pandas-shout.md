---
'repobuddy': minor
---

Add the `code-review` skill. Reviews a change set through three named engineering lenses — Linus, Uncle Bob, and Fowler — running each pass independently and reporting where the verdicts split rather than averaging them into one score.
