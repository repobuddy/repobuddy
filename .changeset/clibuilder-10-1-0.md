---
'repobuddy': patch
'@repobuddy/typescript': patch
---

Bump `clibuilder` to the latest published `^10.1.0` (in-range, non-breaking) picked up
while sweeping `type-plus` across the estate. No source change needed.
